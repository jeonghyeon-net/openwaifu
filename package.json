{
  "scripts": {
    "setup": "mise trust && mise install && npm --prefix 01_BOT install && bash -lc 'set -e; for dir in 02_EXTENSIONS/*/; do [ -f \"$dir/package.json\" ] || continue; (cd \"$dir\" && npm install); done' && mise exec lefthook -- lefthook install",
    "dev": "npm --prefix 01_BOT run dev",
    "start": "npm --prefix 01_BOT run start",
    "check": "npm --prefix 01_BOT run check",
    "test:bot": "npm --prefix 01_BOT test",
    "test:arch": "cd 00_ARCHITECTURE/tests && go test -count=1 ./...",
    "test:extensions": "bash -lc 'set -e; for dir in 02_EXTENSIONS/*/; do [ -f \"$dir/package.json\" ] || continue; (cd \"$dir\" && npm test); done'"
  }
}
