# If pi auth missing, bot will try browser login automatically on startup.
# Manual auth also works: run `pi`, then `/login` and select OpenAI Codex.
DISCORD_BOT_TOKEN=your-discord-bot-token
PI_MODEL=gpt-5.4
PI_THINKING_LEVEL=
